{
  "_about": "Fences lines of real prompts, measured on the live board of 2026-09-10 (MAP F4, NOTE 118 02:49). Only the Fences field is kept.",
  "manager-switchboard-brief": "Write only manager-switchboard/. Read freely: coordinator/ (goals.md and the items: read, never edit or commit), coordinator-as-baseline/, manager-loop-brief/, decide-node/, ux-brief/, control-room-home/, ux-mock/control-room/, docs/, dashboard/src/ui/control-room/, daemon/src/transitions.ts, and the coordinator skill's lane.mjs and page.mjs under ~/.claude/skills/coordinator/ (read only: what the board prints today). Not daemon/, dashboard/, docs/, CLAUDE.md, charter/, the repo-root declaration file, db/. Live beside you: factory-auto-sync-implement (daemon/src worktrees.ts, reviewer.ts, http.ts, transitions.ts, declaration.ts, check.ts, config.ts) and deny-floor-four (daemon/src/settings.ts); never touch, stage or count their files. No workers, no database. Base origin/main at 44e89cc or later, git fetch first; build in your own detached worktree; production SELECT-only; commits on main, you push.",
  "deny-floor-four": "write only daemon/src/settings.ts (the DENY_FLOOR constant), daemon/test/settings.test.ts and deny-floor-four/. Not the rest of settings.ts (buildHookSettings is ledger-per-coder-return-implement's, held), not daemon/src/hooks.ts, daemon/hooks/forward.js, artifacts.ts, daemon.ts, db.ts, picks.ts, api-types.ts, declaration.ts, check.ts, worktrees.ts or reviewer.ts; not supervisor/ (the environment finding is reported, not fixed here); not db/, dashboard/, docs/, CLAUDE.md, charter/, the repo-root declaration file or coordinator/ (read goals.md, never edit or commit it). Live beside you: control-room-drill-ins (dashboard/src and daemon hunks in picks.ts, db.ts, api-types.ts, http.ts, daemon.ts) and fifth-pick-kind (declaration.ts, check.ts). Base ref origin/main after fifth-pick-kind's commit is on it (main is red for tsc before it), git fetch first; build in your own detached worktree; your own supervisor socket path and *_test database; no workers; production SELECT-only; you push.",
  "ledger-per-coder-return-implement": "write only daemon/src/hooks.ts, daemon/src/settings.ts, daemon/hooks/forward.js, daemon/src/artifacts.ts, daemon/src/daemon.ts (the Stop and abandon payloads only), daemon/src/db.ts (the event-type member only, staged by hunk), db/migrations/021_ticket_return_recorded.sql, their tests, and ledger-per-coder-return/. Not daemon/src/picks.ts or api-types.ts (control-room-drill-ins), integration.ts, transitions.ts or gate-unavailable.ts (dead-end-edges), worktrees.ts, declaration.ts, config.ts, reviewer.ts, check.ts or strategies.ts; not dashboard/, docs/, CLAUDE.md, charter/, the repo-root declaration file, or coordinator/ (read goals.md, never edit or commit it). Base ref origin/main after dead-end-edges' (#10) and control-room-drill-ins' (#8) commits are on it, git fetch first; build in your own detached worktree; your own supervisor socket path and *_test database; workers only in probes/sandbox/ on trivial prompts, the reset-away fixtures excepted; production SELECT-only; kill only the pids you spawned, never by pattern; you push.",
  "settled-counter": "write only daemon/src/worktrees.ts, daemon/src/http.ts (the one read of the counter on the shift row), daemon/src/shift-board.sql.ts only if the row type is declared there and only to add the field, daemon/test/worktrees.test.ts, daemon/test/http.test.ts, and settled-counter/. Do not touch daemon/src/daemon.ts, daemon/src/picks.ts, db/, dashboard/ (control-room-home holds all of dashboard/ and .interface-design/), docs/, CLAUDE.md, charter/, or the repo-root declaration file. Base ref origin/main at 6a38b6f; build in your own detached worktree; your own supervisor socket path and your own *_test database; production is SELECT-only.",
  "declared-pick-fields-implement": "write only daemon/src/picks.ts, daemon/src/reviewer.ts, daemon/src/config.ts, daemon/src/declaration.ts, daemon/src/check.ts, daemon/src/db.ts (holdPickDeadlines and the picks it rolls, nothing else), daemon/src/strategies.ts (renderNodeText reuse only, no change to that function), their tests, one fixture under daemon/test/fixtures/, and declared-pick-fields/. Not daemon/src/daemon.ts (build the declaration cache inside picks.ts), not daemon/src/transitions.ts (read and pinned against, never edited), not daemon/src/worktrees.ts, daemon/src/http.ts or daemon/src/shift-board.sql.ts (settled-counter, live), not daemon/src/integration.ts (dead-end-edges, next), not dashboard/ or dashboard/.interface-design/ (control-room-home, live), not db/ (no migration), docs/, CLAUDE.md, charter/, the repo-root nightshift.yml, or coordinator/ (read goals.md, never edit or commit it). Base ref origin/main at 71963ef or later, git fetch first; build in your own detached worktree; your own supervisor socket path and your own *_test database; production is SELECT-only. Shared files with a live lane are staged hunk by hunk."
}
