DECIDE Track A after the gate: declared merge: policy or D20 reviewer first? (rec: reviewer first)
source: darkfactory-compare/REPORT.md open 4

DECIDE Track A after the gate (darkfactory-compare open 4): a declared `merge:` policy with a five-gate deterministic risk classifier (lines ≤200, files ≤10, no protected path, zero fix cycles, zero flags) that advances main from the integration ref, or D20's fresh-context reviewer first? (coordinator recommends: the reviewer first, carrying proposal 1's tool-trace flags, because a merge policy without a reviewer lands unreviewed code on main under O1; the merge: policy follows and consumes those flags; attach-self-check and the D29(b) lane are small and slot in between either way)
