LANE floor-and-sources D29(b) DENY_FLOOR + charter Edit deny; D15 --setting-sources probe; R55 six-cell probe
after: scratch-write-grant

MINE build lane after scratch-write-grant reports, Track A, one prompt: D29 (b) — DENY_FLOOR fifth constant Edit(nightshift.yml), Edit(<charter path>) from the resolved declaration at writeWorkerSettings, pins in settings.test.ts + pool.test.ts; nightshift.yml:162 "rejecting is the human's" corrected per D29 (g); `--setting-sources user` on every spawn (D15) with a probe that the per-worker --settings file and hooks survive it; R55's six-cell probe (Edit/Write/Bash redirect × today's settings/autoAllowBashIfSandboxed) in daemon/scripts/permission-wall.ts

MINE `--setting-sources user`: D15 rules every spawn passes it and no spawn does (integration-ideation open item; a customer repo's .claude/ loads into every worker) — a ruling false of the code; small Track A lane after scratch-write-grant (shares settings.ts spawnArgsFor); needs a probe that the per-worker --settings file and hooks survive the flag
